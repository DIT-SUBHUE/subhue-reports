---
name: gerar-documento
description: Use quando o usuário pedir para gerar relatório, documentação ou dashboard HTML do projeto subhue-reports.
---

Siga a skill canônica compartilhável:

`.agents/skills/gerar-documento/SKILL.md`

Use os schemas definidos em:

`src/subhue_reports/skills/prompts.py`

Use os comandos `just` do repositório para explorar dados, consultar fontes e renderizar o HTML.

Não duplique o fluxo completo neste arquivo.
